# Inside models/staging/schema.yml
version: 2

models:
  - name: stg_customers
    config: 
      materialized: view
      schema: staging
      description: "Cleaned up raw customers data"  
      
  - name: stg_products
    config: 
      materialized: view
      schema: staging
      description: "Cleaned up raw customers data"  

  - name: stg_orders
    config: 
      materialized: view
      schema: staging
      description: "Cleaned up raw customers data"  

  - name: stg_order_items
    config: 
      materialized: view
      schema: public
      description: "Cleaned up raw customers data"         